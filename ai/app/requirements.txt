# Основные зависимости
google-genai>=0.2.0
pydantic>=2.0.0
sentence-transformers>=2.2.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
numpy>=1.24.0

# База данных (опционально, для продакшена можно использовать PostgreSQL)
# Для тестов используется aiosqlite (in-memory SQLite)
aiosqlite>=0.19.0

# Для PostgreSQL (опционально, если используется в продакшене)
# asyncpg>=0.29.0

# HTTP клиент для работы с API CubiCasa5K
httpx>=0.24.0

